<%= render "header" %>

<h1>Authors</h1>

<p>Our blog has <%= User.count %> authors and counting!</p>
<br>

<div class="row row-cols-1 row-cols-md-3 g-4">
    <% @followers.each do |followers| %>
      <div class="col">
        <div class="card text-bg-light mb-3" style="max-width: 18rem;">
          <div class="card-header">Username</div>
          <div class="card-body">
            <p class="card-text">Some other info about the user.</p>
          </div>
        </div>
      </div>
    <% end %>
</div>

<%= render "footer" %>
